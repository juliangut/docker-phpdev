{% extends "docker-entrypoint.twig" %}

{% block user %}

if [ ! -z "$USER_UID" ]; then
  usermod -u $USER_UID jenkins
fi
if [ ! -z "$USER_GID" ]; then
  groupmod -g $USER_GID jenkins
fi
{% endblock %}

{% block command %}
  set -- /usr/sbin/sshd "$@"
{% endblock %}
